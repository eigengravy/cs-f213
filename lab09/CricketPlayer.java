public class CricketPlayer extends Player // A CricketPlayer "Is-A" Player, make changes here!
{
    public CricketPlayer(String name, int maxMatches) { // maxMatches is the maximum number of matches a player can play
        super(name, maxMatches);
    }
}